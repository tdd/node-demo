extends ../layout
include ../../../mixins

block content
  form(method="post", action="/admin/quizzes/#{quiz.id}/questions/#{question.id}")
    input(type="hidden", name="_method", value="put")
    include _form
    button.btn.btn-primary.btn-lg(type="submit") Mettre à jour la question
